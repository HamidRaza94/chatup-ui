import { makeStyles } from '@material-ui/core/styles';

const styles = {
  root: {
    paddingTop: 5,
    paddingBottom: 5,
    background: '#2bffa0',
  },

  row: {
    marginTop: 2,
    marginBottom: 2,
  },

  pointer: {
    cursor: 'pointer',
  },
}

export default makeStyles(styles);
