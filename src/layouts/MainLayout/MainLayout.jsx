import React, { Fragment } from 'react';

import { Heading, Login } from '../../components';

const MainLayout = () => (
  <Fragment>
    <Heading />
    <Login />
  </Fragment>
);

export default MainLayout;
