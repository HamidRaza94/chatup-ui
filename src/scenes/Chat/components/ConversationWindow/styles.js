import { makeStyles } from '@material-ui/core';

const styles = {
  root: {
    height: '90vh',
  },
  conversation: {
    padding: 5,
    flex: 1,
    overflowY: 'auto',
  },
  conversationInput: {
    width: '100%',
  },
};

export default makeStyles(styles);
